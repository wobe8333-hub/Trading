capital_start: 700
capital_target: 10000
leverage: 20
max_capital_usage: 0.25
max_capital_usage_a_plus: 0.30

min_win_rate_threshold: 0.55        # [검증값]
min_rr_threshold: 1.3               # [검증값]
monthly_trade_target: 200           # [검증값]

maker_order_target_ratio: 0.70      # [검증값]

cold_start_trades: 50               # [검증값]
parameter_validation_threshold: 50  # [검증값]
ml_activation_threshold: 500        # [검증값]

api_latency_max_ms: 500             # [검증값]
ws_refresh_max_ms: 100              # [검증값]

max_cost_ratio_vs_tp1: 0.20         # [검증값]
max_slippage_bps: 5                 # [검증값]
liquidity_drop_threshold: 0.40      # [초기값]
spread_expansion_threshold: 2.5     # [초기값]
funding_time_buffer_min: 15         # [초기값]

entry_score_min: 70                 # [초기값]
entry_score_high_risk_min: 75       # [수정] 75~79점 구간 1,075건 해제 / 70~74점은 유지
entry_score_a: 80                   # [초기값]
entry_score_a_plus: 90              # [초기값]

drawdown_alert_pct: 0.15            # [검증값]
drawdown_danger_pct: 0.25           # [검증값]
drawdown_halt_pct: 0.35             # [검증값]

stop_atr_trend: 1.2                 # [수정6] SL 거리 확대
stop_atr_range: 1.2                 # [수정] RANGE RR 3.33→1.67 (tp1_atr_range=2.0 / stop_atr_range=1.2)
stop_atr_expansion: 0.9             # [초기값]
tp1_atr_trend: 2.0                  # [수정] stop=1.2와 동일 → RR=1.0 → 2.0으로 RR=1.667 확보
tp1_atr_range: 2.0                  # [수정] RANGE 레짐 cost_guard 통과
tp1_atr_expansion: 1.5              # [초기값]
tp2_atr_trend: 2.8                  # [수정] tp1(2.0) × 1.4 = 2.8 (RANGE 비율 동일 적용)
tp2_atr_range: 2.8                  # [수정] tp1(2.0) × 1.4 = 2.8 비율 유지
tp2_atr_expansion: 2.2              # [초기값]

scanner_top_n: 10          # [초기값] macro_selector에 전달할 심볼 수 확대
scanner_interval_seconds: 60
scan_symbols: "BTCUSDT,ETHUSDT,SOLUSDT,XRPUSDT,TAOUSDT,DOGEUSDT,XAUTUSDT,RIVERUSDT,HYPEUSDT,SIRENUSDT"  # [수정] 검증된 10개 고정 심볼
scan_top_n_by_volume: 20               # [초기값] 거래량 상위 N개 자동 선정
scan_min_volume_usd: 100000000         # [초기값] 최소 24h 거래량 $1억 이상
scan_symbol_fallback: "BTCUSDT,ETHUSDT,SOLUSDT"  # API 실패 시 fallback
high_beta_threshold: 1.2            # [초기값]
independent_correlation_max: 0.6    # [초기값]
funding_extreme_threshold: 0.05     # [초기값]

trade_sessions:
  - SEOUL
  - LONDON
  - NY
  - OVERLAP
# SEOUL 세션(UTC 00:00~05:59). 주말/평일 구분 없이 적용.
# SEOUL 세션에서는 entry_score_high_risk_min (80) 자동 적용.

timeframe_main: "3m"
timeframe_confirm_fast: "1m"
timeframe_confirm_slow: "5m"

paper_mode: true
live_mode: false

bybit_api_key: "sZe9cSTzzm5W9eoz7L"
bybit_api_secret: "K0XZrXZKx592a03dSLFnHy87M8D19Qe0e68d"
bybit_testnet: false

daily_loss_limit_usd: -35
max_consecutive_losses: 3
